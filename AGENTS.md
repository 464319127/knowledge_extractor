# 项目代理指南

## 开始工作前

修改项目前：

1. 阅读本文件和 `docs/handoff.md`。
2. 修改既有行为前，阅读 `docs/decisions/` 中相关的决策记录。
3. 检查 `TODO.md` 中已跟踪的后续工作。
4. 检查工作区状态，保留用户无关的改动。

## 项目知识

在 Git 中维护简洁、持久的项目知识：

- 默认使用简体中文编写和维护项目知识；代码标识符、命令、路径、协议名及其他不宜翻译的内容保留原文。
- 将长期有效的仓库约定写入本文件。
- 将重要技术决策记录在 `docs/decisions/` 中。每条记录必须包含背景、决策、理由和影响。
- 每完成一个里程碑，以及结束任务、切换主题或可能丢失上下文前，更新 `docs/handoff.md`。
- 将明确的后续工作写入 `TODO.md` 或关联的 issue。
- 让文档与仓库当前状态保持一致。删除或更新已失效的表述，不要累积过时信息。

做出关键决策、解决复杂问题或完成里程碑后，应立即更新项目知识，不要推迟到后续会话。

## 交接要求

保持 `docs/handoff.md` 简洁，并包含：

- 当前目标；
- 已完成的工作；
- 已做出的决策及简要理由；
- 当前状态、阻碍和已知风险；
- 按顺序排列的后续步骤；
- 已实际运行的完整验证命令。

只列出实际运行过的命令，并区分通过、失败和无法运行的检查。

## 文档卫生

- 不保存完整聊天记录。
- 不把推测写成事实。
- 不保留已失效的结论。
- 不写入密钥、凭据、令牌、个人数据或其他敏感信息。
- 优先链接决策记录或 issue，避免重复详细上下文。
- 保持内容客观、可执行，并对下一位贡献者有用。

## 决策记录

遵循 `docs/decisions/README.md` 中的命名和内容规则。仅为具有重要架构、运维、依赖、数据、安全或兼容性影响的选择创建设计决策记录。常规实现细节应放在代码、测试或交接文档中。

## 任务跟踪

使用 `TODO.md` 跟踪尚不需要外部 issue 的仓库内后续任务。每项任务都应有明确结果，并包含足以继续处理的上下文。任务完成且结果已记录到相关代码、文档、决策记录或交接文档后，应将其删除。
