# 待办事项

## 审核首个真实样本

- 审核 `staging/0a7e1ea4/candidates.json` 中的 7 个草稿 Concept。需要取得对话引用的模型源码或对应实验结果，验证显存、算子兼容性和性能结论；只批准有充分依据的 Concept，然后使用 `render` 生成示例 OKF bundle。当前 staging 文件被 Git 忽略，只存在于本地工作区。

这里只添加明确、可执行的后续工作。需要讨论、指定负责人、排期或外部协调的任务应使用 issue 跟踪。
